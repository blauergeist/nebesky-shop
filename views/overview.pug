extends base

block content
    main.main
        .card-container
            each product in products
                .card
                    .card__header
                        .card__picture
                            .card__picture-overlay &nbsp;
                            img.card__picture-img(src=`${product.imageUrl}`, alt=`${product.name}`)
                        h3.heading-tertirary
                            span= product.name

                    .card__details
                        //- h4.card__sub-heading= `${product.difficulty} ${product.duration}-day product`
                        p.card__text= product.description
                        
                        //- .card__data
                        //-     svg.card__icon
                        //-         use(xlink:href='/img/icons.svg#icon-map-pin')
                        //-     span= product.name
                        //- .card__data
                        //-     svg.card__icon
                        //-         use(xlink:href='/img/icons.svg#icon-calendar')
                        //-     span= product.name
                        //- .card__data
                        //-     svg.card__icon
                        //-         use(xlink:href='/img/icons.svg#icon-flag')
                        //-     span= product.name
                        //- .card__data
                        //-     svg.card__icon
                        //-         use(xlink:href='/img/icons.svg#icon-user')
                        //-     span= product.name

                    .card__footer
                        p
                            span.card__footer-value= `$${product.price}`
                        p.card__ratings
                            span.card__footer-value= product.ratingsAverage
                            | 
                            span.card__footer-text= `rating (${product.ratingsQuantity})`
                        button.btn.btn--green.btn--small.add-to-cart(data-product-id=`${product.id}`) Add to cart
                        a.btn.btn--green.btn--secondary(href=`/product/${product.slug}`) Details